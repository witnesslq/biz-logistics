package cc.aisc.logistics.model.acnt.type;

/**
 * Created by sjf on 16-8-5.
 * 结算单位，每公里，每台车，每个地点，每天，每次...
 */
public enum AccountUnit {
    PER_KILOMETER,
    PER_CAR,
    PER_LOCATION,
    PER_DAY,
    PER_TIME;
}
